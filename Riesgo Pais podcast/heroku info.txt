Clone the repository
Use Git to clone safe-refuge-85186's source code to your local machine.

-----------------------------------------------------
$ heroku git:clone -a safe-refuge-85186 (a modificar)
$ cd safe-refuge-85186
---------------------------------------------------------------

Deploy your changes
Make some changes to the code you just cloned and deploy them to Heroku using Git.

----------------------------------------
$ git add .
$ git commit -am "make it better"
$ git push heroku master
-------------------------------------------